import React from 'react';
import { FlatList, StyleSheet, Text, View } from 'react-native';
import { Photo } from '@/data/gallery';
import { PhotoTile } from '@/components/PhotoTile';
export function GalleryGrid({ items, favorites, columns, onPhotoPress, onFavorite, emptyTitle = 'No photos here yet', emptyCopy = 'Your saved moments will appear here.' }: { items: Photo[]; favorites: string[]; columns: 2 | 3; onPhotoPress: (photo: Photo) => void; onFavorite: (id: string) => void; emptyTitle?: string; emptyCopy?: string }) { return <FlatList data={items} key={columns} numColumns={columns} keyExtractor={(item) => item.id} scrollEnabled={items.length > 0} contentContainerStyle={styles.content} columnWrapperStyle={styles.row} renderItem={({ item }) => <PhotoTile source={item.source} title={item.title} aspect={item.aspect} favorite={favorites.includes(item.id)} columns={columns} onPress={() => onPhotoPress(item)} onFavorite={() => onFavorite(item.id)} />} ListEmptyComponent={<View style={styles.empty}><View style={styles.emptyIcon}><View style={styles.emptyDot} /></View><Text style={styles.emptyTitle}>{emptyTitle}</Text><Text style={styles.emptyCopy}>{emptyCopy}</Text></View>} showsVerticalScrollIndicator={false} />; }
const styles = StyleSheet.create({ content: { paddingHorizontal: 20, paddingBottom: 110, flexGrow: 1 }, row: { justifyContent: 'space-between', alignItems: 'flex-start' }, empty: { alignItems: 'center', justifyContent: 'center', paddingTop: 100, paddingHorizontal: 40 }, emptyIcon: { width: 58, height: 58, borderRadius: 20, backgroundColor: '#292723', alignItems: 'center', justifyContent: 'center', marginBottom: 18 }, emptyDot: { width: 22, height: 16, borderRadius: 4, borderWidth: 2, borderColor: '#D99A67' }, emptyTitle: { color: '#F7F4EF', fontSize: 18, fontWeight: '700', marginBottom: 8 }, emptyCopy: { color: '#9A9188', textAlign: 'center', fontSize: 14, lineHeight: 21 } });
